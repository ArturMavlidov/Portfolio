import React, { memo } from "react";

import styles from "./index.module.scss";

const Home = memo(() => {
  return (
    <div className={`page ${styles.home}`}>
      <div>
        
      </div>
    </div>
  );
});

export default Home;
